Analyze the current project and provide a quick health check:

1. Check if the project builds: run the build command from package.json
2. Check for TypeScript errors: `npx tsc --noEmit`
3. Check for lint issues: run the lint command from package.json
4. Check for outdated dependencies: `npm audit`
5. Summarize findings with fix suggestions
